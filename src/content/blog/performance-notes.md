---
title: 首屏性能优化的一些笔记
description: 这是一篇草稿，用来验证 draft 字段在生产构建时会被排除。
pubDate: 2026-08-02
tags: ['性能']
draft: true
---

这是一篇 `draft: true` 的文章。

它应该满足两个条件：

- 在 `npm run dev` 时能在文章列表里看到
- 在 `npm run build` 的产物里**不存在**

如果构建之后还能在 `dist/` 里找到这一页，说明草稿过滤逻辑写错了。

## 正文

正式内容待补。
